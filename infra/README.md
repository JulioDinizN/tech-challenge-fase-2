# Infrastructure

Cloud provisioning code, notes, commands, screenshots, and decisions go here.

Chosen cloud provider: Oracle Cloud Infrastructure.

The deployable Terraform configuration lives in `infra/oci/`. It currently defines the complete OCI resource layer and is intentionally not applied. See `infra/oci/README.md` for architecture, safe validation commands, inputs, costs, and the later deployment workflow.

Keep the OCI setup documented so the final report can explain how the original challenge requirements were mapped to Oracle Cloud services.
