# Scripts

Repeatable commands should be added here as the implementation evolves.

Expected scripts:

- Build all service images.
- Push images to OCIR.
- Apply Kubernetes manifests.
- Run smoke tests.
- Generate load for the autoscaling demo.

The Oracle Cloud CLI is expected to be available locally as `oci`.
